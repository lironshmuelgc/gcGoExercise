#!/usr/bin/env bash
set -e

echo "running formatter (to avoid formatting issues)"
# FILL HERE

echo "running vet (to avoid suspicious constructs)"
# FILL HERE

echo "compiling (to avoid compilation issues)"
# FILL HERE
